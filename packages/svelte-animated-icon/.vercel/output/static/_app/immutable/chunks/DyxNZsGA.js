import{A as e,M as t,ct as n}from"./DURALH8T.js";import"./xihTtKlq.js";var r={title:`Animation Type`,id:6,group:`customization`,description:`Choosing an animation template (draw, spin, jelly, …) and which suit line vs fill icons.`},{title:i,id:a,group:o,description:s}=r,c=t(`<p>The <code>template</code> prop selects which animation runs. Each template is a small WAAPI script that operates on whatever shapes the icon contains - <code>path</code>, <code>circle</code>, <code>line</code>, <code>polyline</code>, <code>polygon</code>, <code>ellipse</code>, <code>rect</code> - so a single template animates <strong>any</strong> of the 4,000+ icons uniformly.</p> <h2>Pick by feel</h2> <table><thead><tr><th>Template</th><th>Vibe</th><th>Best on</th></tr></thead><tbody><tr><td><code>draw</code></td><td>Pen traces the outline</td><td>line icons (regular, light)</td></tr><tr><td><code>native-draw</code></td><td>Same idea, slower</td><td>line icons</td></tr><tr><td><code>cascade</code></td><td>Shapes pop in one after another</td><td>anything with several paths</td></tr><tr><td><code>pop</code></td><td>Each shape bounces in</td><td>mixed / chunky icons</td></tr><tr><td><code>spin</code></td><td>Whole icon rotates</td><td>symmetric icons (gear, loader, settings)</td></tr><tr><td><code>jelly</code></td><td>Squish + wobble</td><td>friendly UI (heart, star, smile)</td></tr><tr><td><code>tada</code></td><td>Quick scale + rotate accent</td><td>celebratory moments (success, share, send)</td></tr><tr><td><code>flip</code></td><td>3D Y-axis flip</td><td>toggleable state (play/pause, on/off)</td></tr><tr><td><code>swing</code></td><td>Pendulum sway</td><td>arrow-like or directional icons</td></tr><tr><td><code>wave</code></td><td>Sequential opacity sweep</td><td>icons with many parallel paths</td></tr><tr><td><code>orbit</code></td><td>Each shape rotates into place</td><td>layered icons (compass, dashboard, sun)</td></tr><tr><td><code>march</code></td><td>Strokes march along the path</td><td>line icons, especially dashed-feeling ones</td></tr><tr><td><code>boil</code></td><td>Bubbling motion</td><td>organic icons (cloud, fire, drop)</td></tr><tr><td><code>glitch</code></td><td>Rapid translate/jitter</td><td>status / debug indicators</td></tr><tr><td><code>wipe</code></td><td>Reveal mask slides across</td><td>line icons, hero moments</td></tr><tr><td><code>rise</code></td><td>Fade + rise from below</td><td>entrances</td></tr><tr><td><code>iris</code></td><td>Circular scale from center</td><td>loading indicators</td></tr><tr><td><code>split</code></td><td>Shape breaks apart and rejoins</td><td>playful / attention-grabbing</td></tr><tr><td><code>drop</code></td><td>Fall + settle</td><td>drop, pin, download</td></tr><tr><td><code>stamp</code></td><td>Slam into place</td><td>confirmations, approvals</td></tr><tr><td><code>trace</code></td><td>Outline-first, then fill</td><td>line → fill transition</td></tr><tr><td><code>assemble</code></td><td>Shapes converge to final position</td><td>composed icons (lock, key)</td></tr></tbody></table> <blockquote><p>Browse them live on the home route or in the <a href="/docs/16-templates-catalog">Templates Catalog</a>.</p></blockquote> <h2>Line vs. fill</h2> <p>Templates that rely on a stroke (like <code>draw</code>, <code>march</code>, <code>native-draw</code>) <strong>only look right on icons that have one</strong>. Phosphor’s <code>regular</code> and <code>light</code> variants are stroked; <code>fill</code> is solid.</p> <table><thead><tr><th>If your icon is…</th><th>Reach for</th></tr></thead><tbody><tr><td>Stroked (<code>regular</code>, <code>light</code>)</td><td><code>draw</code>, <code>native-draw</code>, <code>march</code>, <code>wipe</code>, <code>trace</code></td></tr><tr><td>Solid (<code>fill</code>)</td><td><code>pop</code>, <code>cascade</code>, <code>spin</code>, <code>jelly</code>, <code>tada</code>, <code>drop</code>, <code>stamp</code></td></tr><tr><td>Mixed / unclear</td><td><code>cascade</code>, <code>orbit</code>, <code>split</code>, <code>glitch</code> (geometry-agnostic)</td></tr></tbody></table> <p>Templates declare their preference with a <code>for</code> field - <code>for: 'line'</code> means stroke-based, <code>for: 'fill'</code> means solid-suited, omitted means both. See <code>IconTemplate</code> in <a href="/docs/18-core-exports-and-types">Core Exports and Types</a>.</p> <h2>Combining multiple icons in a row</h2> <p>When a group of icons animate together (a toolbar, a feature row), reach for templates that don’t fight each other. <code>cascade</code> and <code>pop</code> already stagger by index - if you want to stagger across separate icons, drive them with <a href="/docs/08-triggers">controlled triggers</a> and small <code>delay</code> differences in your own state.</p> <h2>Try it on the home page</h2> <p>The interactive explorer on <code>/</code> lets you swap templates, variants, sizes, and triggers on the fly. The fastest way to find the template that <em>feels</em> right for an icon.</p>`,1);function l(t){var r=c();n(22),e(t,r)}export{l as default,r as metadata};