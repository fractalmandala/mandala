import{A as e,M as t,T as n,X as r,Y as i,ct as a,lt as o}from"./DURALH8T.js";import"./xihTtKlq.js";var s={title:`How It Works`,id:23,group:`guides-and-advanced`,description:`The WAAPI template model - what run() does and why there's no animation library dependency.`},{title:c,id:l,group:u,description:d}=s,f=t(`<p>The library is small on purpose. Three pieces do all the work:</p> <ol><li><strong><code>AnimatedIcon.svelte</code></strong> - the Svelte component that owns the SVG and the trigger logic.</li> <li><strong><code>templates.svelte.ts</code></strong> - a flat array of small WAAPI scripts that animate the SVG.</li> <li><strong>Per-icon wrappers</strong> (<code>Gear.svelte</code>, <code>Heart.svelte</code>, …) - thin files that embed the SVG variants and forward everything else.</li></ol> <p>There’s no animation library dependency. Everything runs on the browser’s built-in <a href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API" rel="nofollow">Web Animations API</a>.</p> <h2>Why WAAPI</h2> <p>WAAPI is the browser’s native animation engine - <code>Element.animate(keyframes, options)</code> returns a controllable <code>Animation</code> object. Compared to a JS animation library:</p> <ul><li><strong>No install footprint.</strong> Lighter bundle, fewer version-pinning concerns.</li> <li><strong>Hardware-accelerated.</strong> Transforms and opacity run on the compositor thread by default.</li> <li><strong>Cancellable, reversible, scrubable.</strong> The standard <code>Animation</code> API works on every template’s output.</li> <li><strong>Composites with CSS animations.</strong> Mix WAAPI-driven icons with CSS-driven UI without conflicts.</li></ul> <p>The trade-off: WAAPI doesn’t give you a tween DSL. You write keyframes by hand for each template. That’s a feature here - every template is ~10 lines of readable code.</p> <h2>The template model</h2> <p>A template is one record in <code>TEMPLATES</code>:</p> <pre class="language-ts"></pre> <ul><li><code>id</code> - what you pass to the <code>template</code> prop.</li> <li><code>label</code> - human-readable name.</li> <li><code>for</code> - optional hint: <code>'line'</code> (needs a stroke), <code>'fill'</code> (suited to solid shapes), or omitted (works on both).</li> <li><code>run(svg)</code> - the actual animation. Receives the inner <code>&lt;svg&gt;</code> element, returns the <code>Animation[]</code> it created.</li></ul> <p><code>run</code> is called from <code>startAnimation()</code> inside the component. The component tracks the returned animations so it can cancel them later.</p> <h2>What a template looks like</h2> <p><code>draw</code> - the default - animates each path’s stroke as if a pen were tracing it:</p> <pre class="language-ts"></pre> <p>The shape:</p> <ol><li>Walk every animatable shape inside the SVG.</li> <li>Normalize each one’s path length to <code>1</code> (via <code>pathLength="1"</code>) so <code>stroke-dasharray</code> and <code>stroke-dashoffset</code> are in unit space.</li> <li>Set <code>stroke-dashoffset: 1</code> (line is fully “off”).</li> <li>Animate <code>stroke-dashoffset</code> from <code>1</code> to <code>0</code> - the line draws on.</li> <li>Stagger by index (<code>delay: i * 70</code>) for the cascade feel.</li> <li>Return every animation so the caller can cancel them.</li></ol> <p>When <code>stopAnimation()</code> runs, the component calls each <code>Animation.cancel()</code> and then <code>clearProps(svg)</code> to strip the inline styles the template set.</p> <h2>Why one template works on any icon</h2> <p>Most animation templates operate on shapes inside the SVG using relative geometric transformations (such as scale, rotate, opacity, and stroke). Since these transformations are coordinate-independent or utilize relative units (like setting <code>pathLength="1"</code> for stroke dash offsets), the same templates work seamlessly across different icon sets with different viewBox coordinate spaces (e.g., <code>256</code> for Phosphor, <code>24</code> for Remix/Flowbite/Hero, and <code>512</code> for Ion).</p> <p>This means:</p> <ul><li>New icons “just work” the moment they enter the library.</li> <li>Adding a template benefits every existing icon.</li> <li>The whole library is essentially <code>~4,000+ SVGs × ~22 templates</code>.</li></ul> <h2>The component layer</h2> <p><code>AnimatedIcon.svelte</code> is a Svelte 5 component that:</p> <ol><li>Renders a fixed-viewBox <code>&lt;svg&gt;</code> and injects the <code>svg</code> prop via <code>&#123;@html&#125;</code>.</li> <li>Binds the inner <code>&lt;svg&gt;</code> to a <code>svgEl</code> state.</li> <li>Wires <code>mouseenter</code> / <code>mouseleave</code> to <code>startAnimation</code> / <code>stopAnimation</code> when <code>trigger="hover"</code>.</li> <li>Runs <code>startAnimation</code> in a <code>$effect</code> when <code>trigger="mount"</code> and the SVG is set.</li> <li>Runs <code>startAnimation</code> / <code>stopAnimation</code> in a <code>$effect</code> based on <code>active</code> when <code>trigger="controlled"</code>. The effect’s cleanup calls <code>stopAnimation()</code>, so flipping <code>trigger</code> away from <code>'controlled'</code> (or unmounting) cancels any in-flight - including looping - animation cleanly.</li></ol> <p>The patch layer (<code>loop</code>, <code>speed</code>, <code>easing</code>) is applied <strong>after</strong> the template runs by mutating each animation’s <code>KeyframeEffect.updateTiming()</code> - no template has to know about them.</p> <h2>Custom templates</h2> <p>Adding a new template means adding a record to <code>TEMPLATES</code>:</p> <pre class="language-ts"></pre> <p>Once it’s in the array, <code>template="wiggle"</code> works on every icon and per-icon component. Reach for this when the bundled templates don’t cover a motion you need. See <a href="/docs/18-core-exports-and-types">Core Exports and Types</a> for the API surface.</p> <h2>Why templates live in <code>.svelte.ts</code></h2> <p><code>templates.svelte.ts</code> (note the extension) tells the Svelte compiler to allow runes inside the file. Currently the templates use plain JS - they’re a flat array of records - but the extension leaves room for any future <code>$state</code> or <code>$derived</code> use without a rename.</p>`,1);function p(t){var s=f(),c=r(i(s),18);n(c,()=>`<code class="language-ts"><span class="token keyword">interface</span> <span class="token class-name">IconTemplate</span> <span class="token punctuation">&#123;</span>
  id<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  label<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  <span class="token keyword">for</span><span class="token operator">?</span><span class="token operator">:</span> <span class="token string">'line'</span> <span class="token operator">|</span> <span class="token string">'fill'</span><span class="token punctuation">;</span>
  <span class="token function-variable function">run</span><span class="token operator">:</span> <span class="token punctuation">(</span>svg<span class="token operator">:</span> SVGSVGElement<span class="token punctuation">)</span> <span class="token operator">=></span> Animation<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),o(c);var l=r(c,10);n(l,()=>`<code class="language-ts"><span class="token punctuation">&#123;</span>
  id<span class="token operator">:</span> <span class="token string">'draw'</span><span class="token punctuation">,</span>
  label<span class="token operator">:</span> <span class="token string">'Draw on'</span><span class="token punctuation">,</span>
  <span class="token keyword">for</span><span class="token operator">:</span> <span class="token string">'line'</span><span class="token punctuation">,</span>
  <span class="token function-variable function">run</span><span class="token operator">:</span> <span class="token punctuation">(</span>svg<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">const</span> out<span class="token operator">:</span> Animation<span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    svg<span class="token punctuation">.</span><span class="token function">querySelectorAll</span><span class="token punctuation">(</span><span class="token string">'path,circle,line,polyline,polygon,ellipse,rect'</span><span class="token punctuation">)</span>
      <span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token punctuation">(</span>el<span class="token punctuation">,</span> i<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
        el<span class="token punctuation">.</span><span class="token function">setAttribute</span><span class="token punctuation">(</span><span class="token string">'pathLength'</span><span class="token punctuation">,</span> <span class="token string">'1'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">(</span>el <span class="token keyword">as</span> <span class="token builtin">any</span><span class="token punctuation">)</span><span class="token punctuation">.</span>style<span class="token punctuation">.</span>strokeDasharray <span class="token operator">=</span> <span class="token string">'1'</span><span class="token punctuation">;</span>
        <span class="token punctuation">(</span>el <span class="token keyword">as</span> <span class="token builtin">any</span><span class="token punctuation">)</span><span class="token punctuation">.</span>style<span class="token punctuation">.</span>strokeDashoffset <span class="token operator">=</span> <span class="token string">'1'</span><span class="token punctuation">;</span>
        out<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>
          <span class="token punctuation">(</span>el <span class="token keyword">as</span> <span class="token builtin">any</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">animate</span><span class="token punctuation">(</span>
            <span class="token punctuation">[</span><span class="token punctuation">&#123;</span> strokeDashoffset<span class="token operator">:</span> <span class="token number">1</span> <span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token punctuation">&#123;</span> strokeDashoffset<span class="token operator">:</span> <span class="token number">0</span> <span class="token punctuation">&#125;</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
            <span class="token punctuation">&#123;</span> duration<span class="token operator">:</span> <span class="token number">900</span><span class="token punctuation">,</span> delay<span class="token operator">:</span> i <span class="token operator">*</span> <span class="token number">70</span><span class="token punctuation">,</span> easing<span class="token operator">:</span> <span class="token string">'ease-in-out'</span><span class="token punctuation">,</span> fill<span class="token operator">:</span> <span class="token string">'forwards'</span> <span class="token punctuation">&#125;</span>
          <span class="token punctuation">)</span>
        <span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> out<span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code>`,!0),o(l);var u=r(l,28);n(u,()=>`<code class="language-ts"><span class="token punctuation">&#123;</span>
  id<span class="token operator">:</span> <span class="token string">'wiggle'</span><span class="token punctuation">,</span>
  label<span class="token operator">:</span> <span class="token string">'Wiggle'</span><span class="token punctuation">,</span>
  <span class="token function-variable function">run</span><span class="token operator">:</span> <span class="token punctuation">(</span>svg<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
    svg<span class="token punctuation">.</span>style<span class="token punctuation">.</span>transformOrigin <span class="token operator">=</span> <span class="token string">'center'</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> <span class="token punctuation">[</span>
      svg<span class="token punctuation">.</span><span class="token function">animate</span><span class="token punctuation">(</span>
        <span class="token punctuation">[</span><span class="token punctuation">&#123;</span> transform<span class="token operator">:</span> <span class="token string">'rotate(0)'</span> <span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token punctuation">&#123;</span> transform<span class="token operator">:</span> <span class="token string">'rotate(8deg)'</span> <span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token punctuation">&#123;</span> transform<span class="token operator">:</span> <span class="token string">'rotate(-8deg)'</span> <span class="token punctuation">&#125;</span><span class="token punctuation">,</span> <span class="token punctuation">&#123;</span> transform<span class="token operator">:</span> <span class="token string">'rotate(0)'</span> <span class="token punctuation">&#125;</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
        <span class="token punctuation">&#123;</span> duration<span class="token operator">:</span> <span class="token number">600</span><span class="token punctuation">,</span> easing<span class="token operator">:</span> <span class="token string">'ease-in-out'</span> <span class="token punctuation">&#125;</span>
      <span class="token punctuation">)</span>
    <span class="token punctuation">]</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code>`,!0),o(u),a(6),e(t,s)}export{p as default,s as metadata};