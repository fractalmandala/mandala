import{A as e,M as t,T as n,X as r,Y as i,ct as a,lt as o}from"./DURALH8T.js";import"./xihTtKlq.js";var s={title:`Loop`,id:12,group:`customization`,description:`Forcing a template to repeat forever, and which templates loop by design.`},{title:c,id:l,group:u,description:d}=s,f=t(`<p><code>loop=&#123;true&#125;</code> forces a template to repeat forever instead of running once.</p> <pre class="language-svelte"></pre> <p>This icon starts spinning the moment it mounts and never stops.</p> <h2>How looping works under the hood</h2> <p>When you call <code>startAnimation()</code>, the engine patches each WAAPI animation’s effect timing:</p> <pre class="language-ts"></pre> <p>That tells the browser to repeat each animation indefinitely. The template’s keyframes don’t change - only how many times they’re played.</p> <h2>Templates that look right looping</h2> <table><thead><tr><th>Template</th><th>Loop feel</th></tr></thead><tbody><tr><td><code>spin</code></td><td>Continuous rotation - the classic loader</td></tr><tr><td><code>iris</code></td><td>Pulsing scale - ambient breathing</td></tr><tr><td><code>wave</code></td><td>Sequential opacity sweep - typing indicators</td></tr><tr><td><code>march</code></td><td>Strokes marching - equalizer bars</td></tr><tr><td><code>glitch</code></td><td>Rapid jitter - debug / status</td></tr><tr><td><code>boil</code></td><td>Bubbling motion - organic ambient</td></tr><tr><td><code>orbit</code></td><td>Each shape rotating in place (when each iteration has a visible rest)</td></tr></tbody></table> <h2>Templates that <strong>don’t</strong> loop well</h2> <p>One-shot entrance animations look broken when forced to repeat:</p> <ul><li><code>pop</code>, <code>cascade</code>, <code>rise</code> - already settled; looping jumps back to invisible</li> <li><code>draw</code>, <code>native-draw</code>, <code>wipe</code>, <code>trace</code> - the path is drawn, then redrawn - usable but busy</li> <li><code>drop</code>, <code>stamp</code>, <code>tada</code> - designed as discrete events</li></ul> <p>If you want a perpetual loading-style animation, prefer <code>spin</code>, <code>iris</code>, <code>wave</code>, or <code>march</code>.</p> <h2>Loop with controlled triggers</h2> <p><code>loop</code> works with any trigger, but the combination that surprises people is <code>controlled</code> + <code>loop</code>:</p> <pre class="language-svelte"></pre> <p>The icon loops only while <code>isLoading</code> is <code>true</code>. When the load completes, <code>active</code> flips to <code>false</code>, the animation is cancelled, and the icon resets to its rest state. See <a href="/docs/20-guide-controlled">Parent-Controlled Animation</a> for the full pattern.</p> <h2>Loop cleanup</h2> <p>Switching <code>trigger</code> <em>away</em> from <code>'controlled'</code> while a loop is running triggers an effect cleanup that cancels the animation. You can also call <code>stopAnimation()</code> imperatively. See <a href="/docs/17-imperative-control">Imperative Control</a>.</p> <h2>Performance note</h2> <p>A looping animation runs WAAPI work for as long as it’s active. If you mount many loopers at once (a full-screen spinner grid, for instance), consider:</p> <ul><li>Lower <code>speed</code> if the loop is meant to be ambient - fewer frames per second.</li> <li><code>trigger="controlled"</code> with <code>active</code> only true on visible icons - let <code>IntersectionObserver</code> drive it.</li> <li>Unmount icons that don’t need to be in the DOM.</li></ul>`,1);function p(t){var s=f(),c=r(i(s),2);n(c,()=>`<code class="language-svelte"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>Gear</span> <span class="token attr-name">template</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>spin<span class="token punctuation">"</span></span> <span class="token attr-name">trigger</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>mount<span class="token punctuation">"</span></span> <span class="token attr-name">loop</span> <span class="token punctuation">/></span></span></code>`,!0),o(c);var l=r(c,8);n(l,()=>`<code class="language-ts"><span class="token keyword">if</span> <span class="token punctuation">(</span>loop<span class="token punctuation">)</span> patch<span class="token punctuation">.</span>iterations <span class="token operator">=</span> <span class="token number">Infinity</span><span class="token punctuation">;</span></code>`,!0),o(l);var u=r(l,20);n(u,()=>`<code class="language-svelte"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>Gear</span> <span class="token attr-name">template</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>spin<span class="token punctuation">"</span></span> <span class="token attr-name">trigger</span><span class="token attr-value"><span class="token punctuation">=</span><span class="token punctuation">"</span>controlled<span class="token punctuation">"</span></span> <span class="token attr-name">active=</span><span class="token language-javascript"><span class="token punctuation">&#123;</span>isLoading<span class="token punctuation">&#125;</span></span> <span class="token attr-name">loop</span> <span class="token punctuation">/></span></span></code>`,!0),o(u),a(12),e(t,s)}export{p as default,s as metadata};