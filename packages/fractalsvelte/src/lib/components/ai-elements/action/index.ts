import Action, { type ActionProps } from './action.svelte';
import Actions, { type ActionsProps } from './actions.svelte';

export {
	Action,
	Actions,
	// Aliases
	Action as Item,
	Actions as Root,
	type ActionProps,
	type ActionsProps
};
