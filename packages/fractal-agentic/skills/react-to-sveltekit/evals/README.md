# React-to-SvelteKit contract evals

`evals.json` contains representative prompts for later skill evaluation. The prompts
cover the contract decisions most likely to regress: source classification, route data,
SSR boundaries, and dependency fallbacks.
