---
title: "unique_setup_and_commands"
description: ""
---

No build or test commands. The dataset is consumed by reading the Markdown files (for verse metadata) and the `*_all.csv` files (for bulk row access). Re-importing from Notion regenerates both the per-file Markdowns and the CSV dumps while preserving the hex-id suffixes.