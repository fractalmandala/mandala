{
  "_comment": "Merge the 'skaa' entry below into your MCP client's server config. install.sh prints an exact, path-filled version of this block at the end of a successful install — prefer that output over hand-editing this file.",

  "_where_this_goes": {
    "Claude Desktop (Mac)": "~/Library/Application Support/Claude/claude_desktop_config.json (key: mcpServers)",
    "Claude Desktop (Windows)": "%APPDATA%/Claude/claude_desktop_config.json (key: mcpServers)",
    "Claude Code": "project .mcp.json, or `claude mcp add` — see Claude Code docs (key: mcpServers)"
  },

  "mcpServers": {
    "skaa": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/absolute/path/to/your/project/.skaa/server",
        "skaa_server.py"
      ],
      "env": {
        "SKAA_DB_PATH": "/absolute/path/to/your/project/.skaa/smriti.db",
        "SKAA_SESSION_ID": "default"
      }
    }
  },

  "_venv_fallback": {
    "_comment": "If you installed without uv (plain python3 -m venv fallback), use this form instead:",
    "skaa": {
      "command": "/absolute/path/to/your/project/.skaa/server/.venv/bin/python",
      "args": ["/absolute/path/to/your/project/.skaa/server/skaa_server.py"],
      "env": {
        "SKAA_DB_PATH": "/absolute/path/to/your/project/.skaa/smriti.db"
      }
    }
  }
}
