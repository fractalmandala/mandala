"""
SKAA — a portable, installable smrti (memory) harness for AI agents.

The package name is deliberately kept lowercase/plain in imports; the
Sanskrit terms live in the data model, not in Python identifiers, so the
code stays readable to anyone maintaining it regardless of background.
"""

__version__ = "0.1.0"
