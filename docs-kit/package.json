{
  "name": "docs-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Workspace for the SvelteKit documentation framework.",
  "packageManager": "pnpm@11.0.4",
  "engines": {
    "node": ">=22.13.0",
    "pnpm": ">=11.0.4"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.8",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
