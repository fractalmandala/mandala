---
title: VS Code Workspace Definitions — Unique Setup and Commands
description: Open any .code-workspace file directly in VS Code (File  Open Workspace from File) to launch an IDE session scoped to that single project. Relative path entries resolve against the workspace file's l…
tags: [ide_workspaces]
type: card
module: ide_workspaces
path: ide_workspaces
created: 2026-08-05
updated: 2026-08-06
---

Open any `.code-workspace` file directly in VS Code (`File > Open Workspace from File` to launch an IDE session scoped to that single project. Relative `path` entries resolve against the workspace file's location, so these workspaces must be opened from this directory.
