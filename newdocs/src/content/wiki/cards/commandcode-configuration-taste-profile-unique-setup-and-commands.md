---
title: CommandCode Configuration & Taste Profile — Unique Setup and Commands
description: Permissions in settings.json must be updated whenever new shell commands are needed by orchestration scripts (e.g., adding Shell(grep:), Shell(pnpm:), or full command strings like the Playwright brow…
tags: [commandcode]
type: card
module: commandcode
path: commandcode
created: 2026-08-05
updated: 2026-08-06
---

Permissions in `settings.json` must be updated whenever new shell commands are needed by orchestration scripts (e.g., adding `Shell(grep:*)`, `Shell(pnpm:*)`, or full command strings like the Playwright browser automation and registry health checks already listed).
